package com.stark.service.developerportfolio.service;

import com.stark.service.developerportfolio.model.firebase.UserProfileData;
import org.springframework.stereotype.Service;

@Service
public class UserFetchService implements FetchService<UserProfileData>{

    @Override
    public UserProfileData fetch(String userName) {
        return dataSer
    }
}
